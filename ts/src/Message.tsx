

// THIS IS COMPONENT

//We should write the name of a function with PASCALCasing
function Message()
{
    const name = "souchen";
    if(name)
        return <h1>hello {name}</h1>
    // This is javascript XML (JSX)
    return <h1>Hello soukaina ouchen</h1>;
}
// To use it somewhere we should export it
export default Message;